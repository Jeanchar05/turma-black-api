"use strict";
(()=>{
  const KEYS=["token","adminToken","authToken","accessToken","jwt"],$=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const STORE="study_espelhos_gemeos_v1"; const state={home:null,favorite:false,progress:{example:false,interactive:false,game:false}};
  const getToken=()=>{for(const st of [sessionStorage,localStorage])for(const k of KEYS){try{const v=st.getItem(k);if(v)return v}catch(_){}}return""};
  async function api(path,opts={}){const token=getToken();const r=await fetch(`${location.origin}${path}`,{method:opts.method||"GET",headers:{Accept:"application/json",Authorization:`Bearer ${token}`,...(opts.body?{"Content-Type":"application/json"}:{})},body:opts.body?JSON.stringify(opts.body):undefined,cache:"no-store"});const d=await r.json().catch(()=>({}));if(!r.ok||d.erro)throw new Error(d.erro||d.mensagem||`Erro ${r.status}`);return d}
  function toast(msg){const stack=$("#studyToastStack");if(!stack)return;const el=document.createElement("div");el.className="study-toast";el.textContent=msg;stack.appendChild(el);requestAnimationFrame(()=>el.classList.add("show"));setTimeout(()=>{el.classList.remove("show");setTimeout(()=>el.remove(),200)},2400)}
  function loadStudy(){try{const d=JSON.parse(localStorage.getItem(STORE)||"{}");state.favorite=!!d.favorite;state.progress={...state.progress,...(d.progress||{})}}catch(_){}}
  function saveStudy(){try{localStorage.setItem(STORE,JSON.stringify({favorite:state.favorite,progress:state.progress}))}catch(_){} }
  function progressPct(){return Math.round(Object.values(state.progress).filter(Boolean).length/3*100)}
  function renderStudy(){const pct=progressPct(),done=pct===100,started=pct>0&&pct<100;$("#homeProgress").style.width=`${pct}%`;$("#homeProgressText").textContent=`${pct}% concluído`;$("#homeScore").textContent=`${pct}%`;$("#homeScoreRing").style.setProperty("--score",`${pct}%`);$("#homeCompleted").textContent=done?"1/1":"0/1";$("#homeCompletedSide").textContent=done?"1/1":"0/1";$("#homeStatus").textContent=done?"Concluído":started?"Em andamento":"Disponível";$("#homeStarted").textContent=started?"1":"0";const fav=$("#homeFavorite");fav.setAttribute("aria-pressed",String(state.favorite));fav.title=state.favorite?"Remover dos favoritos":"Favoritar módulo"}
  const first=n=>String(n||"Primo").trim().split(/\s+/)[0]||"Primo";
  const role=v=>({dev:"Equipe",dono:"Equipe",superadmin:"Equipe",admin:"Equipe",suporte:"Suporte",moderador:"Moderador",vendedor:"Vendedor",aluno:"Aluno"}[String(v||"").toLowerCase()]||"Aluno");
  function setAvatar(user){const photo=String(user?.foto||"").trim()||"/assets/default-profile-user.svg";$$('[data-study-avatar]').forEach(el=>{el.textContent="";el.style.backgroundImage=`url("${photo.replaceAll('"','%22')}")`;el.style.backgroundSize="cover";el.style.backgroundPosition="center"})}
  function applyTheme(value){const resolved=value==="system"?(matchMedia("(prefers-color-scheme: light)").matches?"light":"dark"):(value==="light"?"light":"dark");document.documentElement.dataset.theme=resolved;const use=$("#studyThemeToggle use");if(use)use.setAttribute("href",`assets/dashboard-icons.svg#${resolved==="dark"?"i-moon":"i-sun"}`)}
  async function toggleTheme(){const next=(document.documentElement.dataset.theme||"dark")==="dark"?"light":"dark";applyTheme(next);try{await api("/dashboard-premium/preferencias",{method:"PUT",body:{tema:next}})}catch(_){} }
  function fill(){const home=state.home||{},user=home.usuario||{},plan=home.plano||{};$$('[data-study-name]').forEach(el=>el.textContent=first(user.nome));$$('[data-study-role]').forEach(el=>el.textContent=role(user.cargo||user.tipo));setAvatar(user);$("#studyPlanName").textContent=plan.nome||"Plano Premium";$("#studyPlanLabel").textContent=plan.rotulo||"Acesso completo";const unread=Number(home.notificacoesNaoLidas||0),badge=$("#studyNotificationBadge");badge.hidden=unread<=0;badge.textContent=String(unread)}
  function openSidebar(){$("#studySidebar")?.classList.add("open");const o=$("#studyMobileOverlay");if(o)o.hidden=false;document.body.style.overflow="hidden"}
  function closeSidebar(){$("#studySidebar")?.classList.remove("open");const o=$("#studyMobileOverlay");if(o)o.hidden=true;document.body.style.removeProperty("overflow")}
  function logout(){for(const st of [sessionStorage,localStorage])for(const k of KEYS){try{st.removeItem(k)}catch(_){}}location.replace("/")}
  function register(){$("#studyMenuToggle")?.addEventListener("click",openSidebar);$("#studyMobileOverlay")?.addEventListener("click",closeSidebar);$("#studyThemeToggle")?.addEventListener("click",toggleTheme);$("#studyLogout")?.addEventListener("click",logout);$$("#studySidebar a").forEach(a=>a.addEventListener("click",closeSidebar));$("#homeFavorite")?.addEventListener("click",e=>{e.preventDefault();e.stopPropagation();state.favorite=!state.favorite;saveStudy();renderStudy();toast(state.favorite?"Módulo Gêmeos adicionado aos favoritos.":"Módulo removido dos favoritos.")});$$('[data-study-filter]').forEach(b=>b.addEventListener("click",()=>{$$('[data-study-filter]').forEach(x=>x.classList.remove("active"));b.classList.add("active");const f=b.dataset.studyFilter,p=progressPct();const card=$("#gemeosModuleCard");card.hidden=(f==="progress"&&(p===0||p===100))||(f==="done"&&p<100)||(f==="favorites"&&!state.favorite)}));document.addEventListener("keydown",e=>{if(e.key==="Escape")closeSidebar()})}
  async function init(){loadStudy();renderStudy();register();try{state.home=await api("/dashboard-premium/home");applyTheme(state.home?.preferencias?.tema||"dark");fill()}catch(_){applyTheme("dark")}}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init();
})();