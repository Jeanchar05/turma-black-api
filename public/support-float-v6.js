"use strict";
(() => {
  if (window.__TURMA_SUPPORT_FLOAT_V6__) return;
  window.__TURMA_SUPPORT_FLOAT_V6__ = true;
  const STORAGE_KEY = "turma_support_float_v6";
  const TOKEN_KEYS = ["token", "adminToken", "authToken", "accessToken", "jwt"];
  const $ = (selector, root = document) => root.querySelector(selector);
  const esc = (value) => String(value ?? "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
  const state = { ticket: null, ticketId: "", tickets: [], poller: null, suspended: false, minimized: true, open: false, unread: 0, lastMessageId: "", hiddenTicketId: "" };

  function token(){for(const storage of [sessionStorage,localStorage])for(const key of TOKEN_KEYS){try{const value=storage.getItem(key);if(value)return value}catch(_){}}return ""}
  async function api(path,options={}){const jwt=token();if(!jwt)throw new Error("Sessão expirada.");const response=await fetch(path,{method:options.method||"GET",headers:{Accept:"application/json",Authorization:`Bearer ${jwt}`,...(options.body!==undefined?{"Content-Type":"application/json"}:{})},body:options.body!==undefined?JSON.stringify(options.body):undefined,cache:"no-store"});const data=await response.json().catch(()=>({}));if(!response.ok||data.erro)throw new Error(data.erro||data.mensagem||`Erro ${response.status}`);return data}
  function isActive(ticket){return Boolean(ticket?.id)&&!["resolvido","fechado"].includes(String(ticket.status||"").toLowerCase())}
  function loadUiState(){try{const saved=JSON.parse(localStorage.getItem(STORAGE_KEY)||"{}");state.suspended=Boolean(saved.suspended);state.minimized=saved.minimized!==false;state.open=Boolean(saved.open);state.ticketId=String(saved.ticketId||"");state.lastMessageId=String(saved.lastMessageId||"");state.hiddenTicketId=String(saved.hiddenTicketId||"")}catch(_){}}
  function saveUiState(){try{localStorage.setItem(STORAGE_KEY,JSON.stringify({suspended:state.suspended,minimized:state.minimized,open:state.open,ticketId:state.ticket?.id||state.ticketId||"",lastMessageId:state.lastMessageId||"",hiddenTicketId:state.hiddenTicketId||""}))}catch(_){}}
  function formatDate(value){const date=new Date(value||"");if(Number.isNaN(date.getTime()))return "";return new Intl.DateTimeFormat("pt-BR",{day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit"}).format(date)}
  function statusLabel(status){return{aberto:"Aberto",em_atendimento:"Em atendimento",respondido:"Respondido",resolvido:"Resolvido",fechado:"Fechado"}[status]||"Atendimento"}
  function activeTicket(list){const open=(Array.isArray(list)?list:[]).filter(isActive);if(state.ticketId)return open.find(item=>item.id===state.ticketId)||open[0]||null;return open[0]||null}

  function inject(){
    if($("#supportFloatV6"))return;
    const host=document.createElement("div");host.id="supportFloatV6";host.className="support-float-v6";
    host.innerHTML=`<button class="support-float-launcher" id="supportFloatLauncher" type="button" aria-label="Abrir atendimento" hidden><span class="support-float-pulse"></span><b>?</b><span>Suporte</span><i id="supportFloatBadge" hidden>0</i></button><section class="support-float-panel" id="supportFloatPanel" hidden aria-label="Chat de suporte"><header class="support-float-head"><div><span id="supportFloatCode">SUPORTE</span><strong id="supportFloatTitle">Atendimento</strong><small id="supportFloatStatus">Carregando…</small></div><div class="support-float-controls"><button type="button" data-support-action="suspend" title="Suspender atualizações">Ⅱ</button><button type="button" data-support-action="minimize" title="Deixar no canto">—</button><button type="button" data-support-action="hide" title="Ocultar este chamado">⌫</button><button type="button" data-support-action="close" title="Fechar">×</button></div></header><div class="support-float-suspended" id="supportFloatSuspended" hidden><span>Ⅱ</span><div><strong>Atendimento suspenso</strong><small>As atualizações automáticas estão pausadas.</small></div><button type="button" data-support-action="resume">Retomar</button></div><div class="support-float-body" id="supportFloatBody"><div class="support-float-empty">Carregando conversa…</div></div><form class="support-float-reply" id="supportFloatReply"><textarea id="supportFloatInput" maxlength="3000" rows="1" placeholder="Escreva uma mensagem…"></textarea><button type="submit" aria-label="Enviar">➤</button></form><footer><a href="/suporte">Abrir Central de Suporte ↗</a><small id="supportFloatFooter">Atualização automática ativa</small></footer></section>`;
    document.body.appendChild(host);
    $("#supportFloatLauncher")?.addEventListener("click",()=>{if(!isActive(state.ticket))return;state.open=true;state.minimized=false;state.hiddenTicketId="";saveUiState();renderVisibility();refreshTicket(true)});
    host.addEventListener("click",event=>{const action=event.target.closest("[data-support-action]")?.dataset.supportAction;if(!action)return;if(action==="suspend"){state.suspended=true;state.open=true;state.minimized=false;stopPolling()}if(action==="resume"){state.suspended=false;state.open=true;state.minimized=false;refreshTicket(true);startPolling()}if(action==="minimize"){state.minimized=true;state.open=false}if(action==="close"){state.open=false;state.minimized=true;state.unread=0}if(action==="hide"){state.hiddenTicketId=state.ticket?.id||"";state.open=false;state.minimized=true;state.unread=0}saveUiState();renderVisibility()});
    $("#supportFloatReply")?.addEventListener("submit",sendReply);$("#supportFloatInput")?.addEventListener("keydown",event=>{if(event.key==="Enter"&&!event.shiftKey){event.preventDefault();$("#supportFloatReply")?.requestSubmit()}});
  }

  function renderVisibility(){
    const panel=$("#supportFloatPanel"),launcher=$("#supportFloatLauncher");if(!panel||!launcher)return;
    const active=isActive(state.ticket), hiddenForTicket=active&&state.hiddenTicketId===state.ticket.id;
    if(!active){state.open=false;state.minimized=true}
    panel.hidden=!active||hiddenForTicket||!state.open||state.minimized;
    launcher.hidden = !state.ticket || !active || hiddenForTicket || (state.open && !state.minimized);
    $("#supportFloatSuspended").hidden=!state.suspended;$("#supportFloatReply").hidden=state.suspended||!active;$("#supportFloatFooter").textContent=state.suspended?"Atualizações pausadas":"Atualização automática ativa";
    const badge=$("#supportFloatBadge");if(badge){badge.hidden=state.unread<=0;badge.textContent=String(Math.min(99,state.unread))}
  }
  function renderTicket(){
    const ticket=state.ticket;if(!isActive(ticket)){state.ticket=null;state.ticketId="";state.suspended=false;renderVisibility();saveUiState();return}
    $("#supportFloatCode").textContent=`CHAMADO #${ticket.id.slice(-6).toUpperCase()}`;$("#supportFloatTitle").textContent=ticket.assunto||"Atendimento";$("#supportFloatStatus").textContent=`${statusLabel(ticket.status)} • ${ticket.atendenteNome?`com ${ticket.atendenteNome}`:"aguardando equipe"}`;
    const messages=Array.isArray(ticket.respostas)?ticket.respostas:[],body=$("#supportFloatBody");body.innerHTML=messages.length?messages.map(message=>{const mine=message.tipo==="usuario",system=message.tipo==="sistema";return `<article class="support-float-message ${mine?"mine":"team"} ${system?"system":""}"><strong>${esc(system?"Sistema":mine?"Você":message.autorNome||"Equipe Turma do Primo")}</strong><p>${esc(message.mensagem).replaceAll("\n","<br>")}</p><small>${esc(formatDate(message.criadoEm))}</small></article>`}).join(""):'<div class="support-float-empty">A conversa aparecerá aqui.</div>';
    const last=messages[messages.length-1];if(last?.id&&state.lastMessageId&&last.id!==state.lastMessageId&&!state.open&&last.tipo==="equipe")state.unread+=1;if(last?.id)state.lastMessageId=last.id;saveUiState();if(!state.minimized)requestAnimationFrame(()=>{body.scrollTop=body.scrollHeight});renderVisibility();
  }
  async function loadTickets(){try{const data=await api("/meus-chamados");state.tickets=Array.isArray(data.chamados)?data.chamados:[];const chosen=activeTicket(state.tickets);if(!chosen){state.ticket=null;state.ticketId="";renderTicket();return}if(state.hiddenTicketId&&state.hiddenTicketId!==chosen.id)state.hiddenTicketId="";state.ticketId=chosen.id;await refreshTicket(false)}catch(_){}}
  async function refreshTicket(force=false){if(state.suspended&&!force)return;const id=isActive(state.ticket)?state.ticket.id:state.ticketId||activeTicket(state.tickets)?.id;if(!id)return loadTickets();try{const data=await api(`/suporte/${encodeURIComponent(id)}`);state.ticket=isActive(data.chamado)?data.chamado:null;state.ticketId=state.ticket?.id||"";renderTicket()}catch(_){}}
  async function sendReply(event){event.preventDefault();if(!isActive(state.ticket)||state.suspended)return;const input=$("#supportFloatInput"),message=String(input?.value||"").trim();if(!message)return;const button=$("#supportFloatReply button");button.disabled=true;try{await api(`/suporte/${encodeURIComponent(state.ticket.id)}/responder`,{method:"POST",body:{mensagem:message}});input.value="";await refreshTicket(true)}catch(_){input.placeholder="Não foi possível enviar. Tente novamente."}finally{button.disabled=false}}
  function stopPolling(){clearInterval(state.poller);state.poller=null}function startPolling(){stopPolling();if(state.suspended)return;state.poller=setInterval(()=>{if(!document.hidden)refreshTicket(false)},state.open&&!state.minimized?5000:12000)}
  async function init(){if(!token())return;loadUiState();inject();renderVisibility();await loadTickets();if(state.open&&!state.minimized&&isActive(state.ticket))await refreshTicket(true);startPolling();document.addEventListener("visibilitychange",()=>{if(!document.hidden&&!state.suspended)refreshTicket(false)});document.addEventListener("turma:support-ticket-opened",()=>{state.hiddenTicketId="";loadTickets()})}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init();
})();