"use strict";(()=>{const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const wheel=[0,32,15,19,4,21,2,25,17,34,6,27,13,36,11,30,8,23,10,5,24,16,33,1,20,14,31,9,22,18,29,7,28,12,35,3,26],red=new Set([1,3,5,7,9,12,14,16,18,19,21,23,25,27,30,32,34,36]);const terminal=n=>n===0?0:n%10;function targets(a,b){if(a==null||b==null)return[];const hi=Math.max(a,b),lo=Math.min(a,b),sum=hi+lo,diff=hi-lo,o=[diff];if(sum<=36)o.push(sum);return[...new Set(o)]}function neigh(n,k){const i=wheel.indexOf(n),o=[];if(i<0)return o;for(let d=-k;d<=k;d++)o.push(wheel[(i+d+wheel.length)%wheel.length]);return o}function renderRace(id,ts=[]){const r=$(id);r.innerHTML="";const marked=new Set(ts.flatMap(n=>neigh(n,2)));wheel.forEach(n=>{const b=document.createElement("button");b.className="race-num "+(n===0?"green":red.has(n)?"red":"")+(marked.has(n)?" target":"");b.textContent=n;r.appendChild(b)})}function pair(v){if(v.length<2)return"—";const t=targets(v[0],v[1]);return`${v[0]} + ${v[1]} → ${t.join(" / ")||"sem alvo"}`}function recalc(){const vals=$$("#fibInputs input").map(i=>i.value===""?null:+i.value).filter(v=>v!==null&&v>=0&&v<=36),first=vals.slice(0,2),last=vals.slice(-2),st=first.length===2?targets(...first):[],en=last.length===2?targets(...last):[];$("#fibStart").textContent=pair(first);$("#fibEnd").textContent=pair(last);const exact=st.filter(n=>en.includes(n)),terms=[...new Set(st.map(terminal).filter(t=>en.map(terminal).includes(t)))];$("#fibCong").textContent=exact.length?`Número ${exact.join(", ")}`:terms.length?`Terminal ${terms.join(", ")}`:"—";const all=[...new Set([...st,...en])];$("#fibTargets").textContent=all.length?all.join(" · "):"—";renderRace("#fibRace",all)}$$('.strategy-tabs button').forEach(b=>b.onclick=()=>{$$('.strategy-tabs button').forEach(x=>x.classList.remove('active'));$$('.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#'+b.dataset.panel).classList.add('active')});$("#studyMenuToggle")?.addEventListener("click",()=>$("#studySidebar")?.classList.toggle("open"));for(let i=0;i<12;i++){const input=document.createElement("input");input.type="number";input.min=0;input.max=36;input.placeholder=String(i+1);input.addEventListener("input",recalc);$("#fibInputs").appendChild(input)}renderRace("#fibRace");let spins=[];function gameRender(){const root=$("#fibGameTimeline");root.innerHTML="";for(let i=0;i<12;i++){const box=document.createElement("input");box.disabled=true;box.value=spins[i]??"";box.placeholder=String(i+1);root.appendChild(box)}const first=spins.slice(0,2),last=spins.slice(-2),all=[...new Set([...(first.length===2?targets(...first):[]),...(last.length===2?targets(...last):[])])];$("#fibGameTargets").textContent=all.length?"Alvos: "+all.join(" · "):"Sem alvos";$("#fibGameStatus").innerHTML=spins.length<2?"Adicione pelo menos 2 giros.":`Timeline com <strong>${spins.length}/12</strong> giros. Cálculo atualizado.`;renderRace("#fibGameRace",all)}$("#fibAdd").onclick=()=>{const v=+$("#fibSpin").value;if(!Number.isInteger(v)||v<0||v>36)return;spins.push(v);if(spins.length>12)spins.shift();$("#fibSpin").value="";gameRender()};$("#fibClear").onclick=()=>{spins=[];gameRender()};renderRace("#fibGameRace");gameRender()})();